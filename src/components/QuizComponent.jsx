import React, { useState } from "react";
import DynamicForm from './DynamicForm'
import {
  Typography,
  FormControl,
  InputLabel,
  Select,
  MenuItem,
  TextField,
  Button,
  Box,
} from "@mui/material";

const QuizComponent = () => {

  return (
    <>
    <DynamicForm/>
    </>
  );
};

export default QuizComponent;
